/*
 * Owner: terry@kupotech.com
 */

import { lazyImageObserver } from 'utils/initLazyImgObserver';

describe('utils/initLazyImgObserver', () => {

  it('should define', () => {
    expect(lazyImageObserver).toBeDefined();
  })
})