/**
 * Owner: garuda@kupotech.com
 */

import SyncStorage, {
  brandPrefix,
  namespace,
  isSupportStorageType,
  defaultStorageType,
} from '../packages/syncStorage';

export { brandPrefix, namespace, isSupportStorageType, defaultStorageType, SyncStorage };
