import { createContext } from 'react';
/**
 * 主题context
 */
export const FuturesContext = createContext(undefined);
/**
 * 合约配置context
 */
export const FuturesSymbolsMapContext = createContext({});
export const DispatchContext = createContext(undefined);
