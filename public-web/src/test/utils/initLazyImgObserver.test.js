/**
 * Owner: jessie@kupotech.com
 */
import { lazyImageObserver } from 'utils/initLazyImgObserver';

describe('lazyImageObserver', () => {
  it('lazyImageObserver should return null', () => {
    expect(lazyImageObserver).toBe(null);
  });
});
