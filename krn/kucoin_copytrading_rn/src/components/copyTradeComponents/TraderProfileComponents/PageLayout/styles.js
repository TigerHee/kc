import styled from '@emotion/native';

export const FooterArea = styled.View`
  padding: 16px;
  background: ${({theme}) => theme.colorV2.overlay};
`;

export const PageLayoutWrap = styled.View`
  background: ${({theme}) => theme.colorV2.overlay};
  flex: 1;
  padding-bottom: 12px;
`;
