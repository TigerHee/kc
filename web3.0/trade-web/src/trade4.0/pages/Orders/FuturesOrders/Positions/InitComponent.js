/**
 * Owner: charles.yang@kupotech.com
 */

import React, { memo } from 'react';
import useInitTradePassword from '../hooks/common/useInitTradePassword';

const InitComponent = () => {
  // usePositionsInit();
  // useInitTradePassword();
  return null;
};

export default memo(InitComponent);
