/**
 * Owner: charles.yang@kupotech.com
 */

import React, { memo } from 'react';
import useOpenOrdersInit from './useOpenOrdersInit';

const InitComponent = () => {
  // useOpenOrdersInit();
  return null;
};

export default memo(InitComponent);
