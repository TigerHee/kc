/**
 * Owner: charles.yang@kupotech.com
 */
import { createContext } from 'react';

// wrapper context
export const WrapperContext = createContext('');
export const name = 'realizedPNL';

export const eventName = `screen_${name}_change`;
