/**
 * Owner: victor.ren@kupotech.com
 */
import React from 'react';
import { storiesOf } from '@storybook/react';
import Button from '../doc/button';

storiesOf('Button', module).add('basic', () => {
  return <Button />;
});
