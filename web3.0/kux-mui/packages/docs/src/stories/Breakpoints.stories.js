/**
 * Owner: victor.ren@kupotech.com
 */
import React from 'react';
import { storiesOf } from '@storybook/react';
import Box from '../doc/breakpoints';

storiesOf('Breakpoints', module).add('basic', () => {
  return <Box />;
});
