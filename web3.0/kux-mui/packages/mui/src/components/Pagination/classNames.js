/**
 * Owner: victor.ren@kupotech.com
 */
import { generateClassName } from 'styles/index';

export default function getPaginationClassName(slot) {
  return generateClassName('KuxPagination', slot);
}
