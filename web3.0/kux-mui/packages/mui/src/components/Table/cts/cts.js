/**
 * Owner: victor.ren@kupotech.com
 */
import React from 'react';

export const TableContext = React.createContext(false);

export const TableBodyContext = React.createContext(false);

export const TableStickyContext = React.createContext(false);

export const TableResizeContext = React.createContext(false);

export const TableRowHoverContext = React.createContext(false);

export const TableExpandedRowContext = React.createContext(false);
