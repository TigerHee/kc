import React from 'react';
// import Detail from 'src/components/Assets/Profit/Detail';
import { customRender } from 'test/setup';

// todo  Cannot find module '@kc/mui/lib/ClickAwayListener'

describe('test Detail', () => {
  test('test Detail', () => {
    // const { container, rerender } = customRender(<Detail showAssets={false} />, {
    //   overview: {},
    //   user_assets: {},
    //   accountRecord: {},
    // });
    // expect(container.innerHTML).toContain('******');
    // rerender(<Detail prompt={{ content: 'test' }} />);
  });
});
